{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main", "settings_window"],
  "permissions": [
    "core:default",
    "opener:default",
    "store:default",
    "core:window:allow-close"
  ]
}
